import { Module } from '@nestjs/common';

@Module({
  // TODO: Add profile entities when they are created
  // imports: [TypeOrmModule.forFeature([ReferrerProfile, CandidateProfile])],
  // exports: [TypeOrmModule],
})
export class ProfilesModule {}
